# 📖 ما يجب قراءته أولاً (الترتيب الإلزامي)

قبل البدء في أي عمل برمجي، اقرأ هذه الوثائق بهذا الترتيب:
1. `team_package/docs/PROJECT_CONSTITUTION.md` (دستور وقوانين النظام).
2. `team_package/api_contract/API_SPECIFICATION.md` (عقود الـ Endpoints المركزية).
3. `team_package/database/database_dictionary.md` (قاموس الجداول والعلاقات في PostgreSQL).
4. `team_package/security/SECURITY_SPECIFICATION.md` (معايير الأمان والتشفير).
5. `backend/prisma/schema.prisma` (مخطط النماذج الأكاديمية الحالي).
